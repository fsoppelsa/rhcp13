# An environment that can be used with the deployed-server.yaml template to do
# initial bootstrapping of the deployed servers.
resource_registry:
  OS::TripleO::DeployedServer::Bootstrap: /home/stack/output/deployed-server/deployed-server-bootstrap-rhel.yaml

parameter_defaults:
  EnablePackageInstall: True
